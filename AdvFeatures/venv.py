# python3 -m venv venv -> Create a venv
# source venv/bin/active -> Activate it
# pip install --upgrade pip -> upgrade pip version
# pip install <package_name> -> Install modules

# pip freeze -> Show the installed packages
# pip freeze > requirements.txt -> Write the modules with version
# pip install -r .\requirements.txt -> Install all the packages at once